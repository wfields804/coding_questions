#Question 1 - Write a funtion to print "hello_USERNAME!" USERNAME is the input of the fucntion. The first line of the code has been defined as below.
def hello_name(wfields):
    print("hello " + wfields)

hello_name("wfields")



#Question 2 - Write a python function, first_odds that prints the odd numbers from 1-100 and returns nothing
def first_odds():
    print(list(range(1,100,2)))

first_odds()



#Question 3  - Please write a Python function, max_num_in_list to return the max number of a given list. The first line of code has been defines as below.
def max_num_in_list(a_list):
    return print(max(a_list))
max_num_in_list([1, 8, 6, 8, 3, 9, 11])

#Question 4

def is_leap(a_year):
    if a_year % 4 == 0 and a_year % 100 !=0:
        print(f"{a_year} is a leap year")
    elif a_year % 400 == 0:
        print(f"{a_year}")
    else:
        a_year = False
        print(f"{a_year}")


#Question 5

def is_consecutive(a_list):
    i = 0
    status = True
    while i < len(a_list) - 1:
        if a_list [i] + 1 == a_list[i + 1]:
            i += 1
        else:
            status = False
            break
        print(status)
        

